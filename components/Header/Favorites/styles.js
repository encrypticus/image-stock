import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  root: {
    marginLeft: 'auto',
  },
  icon: {
    marginRight: 7,
    width: 28,
    height: 'auto',
  },
  link: {
    display: 'flex',
    alignItems: 'center',
    fontSize: 18,
  },
});
