import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  root: {
    display: 'flex',
    alignItems: 'center',
    fontSize: 24,
    fontWeight: 700,
  },
  icon: {
    marginRight: 24,
  },
  link: {
    display: 'flex',
    alignItems: 'center',
  },
});
