import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  root: {
    backgroundColor: '#000',
    padding: '90px 20px',
  },
  inner: {
    display: 'flex',
    alignItems: 'center',
    marginBottom: 30,
  },
});
