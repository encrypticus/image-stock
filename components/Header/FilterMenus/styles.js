import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles({
  menuItem: {
    paddingTop: 0,
    paddingBottom: 0,
    paddingRight: 0,
  },
  label: {
    marginRight: 0,
    paddingRight: 16,
  },
});
